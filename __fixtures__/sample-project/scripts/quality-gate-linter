#!/usr/bin/env bash
set -euo pipefail

# Thin wrapper: runs linter with warnings-as-errors
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$PROJECT_DIR"

npx eslint . --max-warnings=0 2>&1
